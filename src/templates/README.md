# Template registry (M3)

Friendly-name → deck-ID registry stored at `~/.config/slides-mcp/templates.json`
(PRD §7.5): `register_template` / `list_templates` tools, extended-tier
instantiation via `files.copy`, diagnostics verification.

Tracked in issue #35 under epic #3.
