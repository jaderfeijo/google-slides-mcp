# Setup engine (M2)

The Claude-guided setup engine lands here (PRD §5): persisted state machine
(`~/.config/slides-mcp/state.json`), `get_setup_status` / `run_setup_step` /
`run_diagnostics` tools, gcloud automation with absolute-path probing, guided
console steps, and paste-first client JSON import.

Tracked in epic #2 (issues #21–#28).
